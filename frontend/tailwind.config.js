/** @type {import('tailwindcss').Config} */
// Colors, font, animations, and dark mode variant are all configured in index.css.
// This file only declares content sources for the JIT scanner.
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
};
